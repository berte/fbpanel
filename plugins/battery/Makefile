## miniconf makefiles ## 1.1 ##

TOPDIR := ../..

battery_src = battery.c power_supply.c
battery_cflags = -DPLUGIN $(GTK3_CFLAGS)
battery_libs = $(GTK3_LIBS)
battery_type = lib

include $(TOPDIR)/.config/rules.mk
